[pytest]
asyncio_mode = auto
pythonpath = .
addopts =
    -v
    --tb=short
    --cov=app
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-fail-under=70
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
