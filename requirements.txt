# YAML support
PyYAML==6.0.1  # PyYAML: used by app/services/text_service.py

# FastAPI + ASGI server
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Settings management
pydantic-settings==2.2.0

# Email validation (used by Pydantic EmailStr)
email-validator==2.1.1

# AI / keyword extraction
groq==0.9.0
yake==0.4.8

# Emoji handling
emoji==2.12.0

# Database (async SQLAlchemy + PostgreSQL)
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# Payments
setuptools==75.8.0  # pinned <78: razorpay uses pkg_resources, removed in setuptools>=78
razorpay==1.4.2

# Structured logging
python-json-logger==2.0.7

# Testing
pytest==8.1.0
pytest-asyncio==0.23.0
httpx==0.27.0         # async test client for FastAPI
pytest-cov==5.0.0
